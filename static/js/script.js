// Optional JavaScript for smooth animations or interactive elements
// For example, adding a smooth scroll effect to "View Project" links

document.querySelectorAll('.project-card a').forEach(item => {
    item.addEventListener('click', event => {
        // Here, you can add any action like navigating to the project details page
        alert('Navigating to project details...');
    });
});
